// This is not autogenerated.
// Thin re-export wrapper so that hand-written modules (e.g. future injectable
// endpoint factories) can import from `./cloudApi` — a path that matches the
// published entry name (`dist/cloudApi.*`) — rather than from the generated
// `./cloud` module, whose basename does not match the tsup output name.
//
// The code-generator writes `cloud.ts` directly; this wrapper is kept in a
// separate file so that regenerating `cloud.ts` never overwrites this entry-
// name alignment shim.
export * from "./cloud";
